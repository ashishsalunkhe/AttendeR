<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app.attender">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ANewTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/ANewTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StudentInfo"
            android:label="@string/register_text" />
        <activity
            android:name=".PersonalInfo"
            android:label="@string/register_text" />
        <activity
            android:name=".SignIn"
            android:label="@string/sign_in_text"
            />
        <activity android:name=".QRScanner" />
        <activity android:name=".RegisterInfo">

        </activity>
        <activity android:name=".Scan_Start">

        </activity>
    </application>

</manifest>